// Example 1 :- Return the length of the last word in the string
let s = "Hello World"
let lastword = (s.substring(6,11)) // Output is World
let count = 0
for ( let i = 0; i<lastword.length; i++)
{
  count++ // count = count + 1
}
console.log(" Print the Length for the last world in the given string  : " + count)
